import {test, expect} from '@playwright/test'
import {setupTestHooks} from './helpers'

setupTestHooks()

// ─── Smoke tests ─────────────────────────────────────────────────────────────
// Each test loads the example, waits for _testFinish, and takes an initial screenshot.
// The beforeEach hook handles all of this — the test body just verifies the page title.

test('3d-tiles-renderer', async({page}) => expect(page).toHaveTitle('3d Tiles Renderer'))
test('directional-light', async({page}) => expect(page).toHaveTitle('Directional Light'))
test('drc-load', async({page}) => expect(page).toHaveTitle('DRACO(DRC) Load'))
test('dropzone-plugin', async({page}) => expect(page).toHaveTitle('Dropzone Plugin'))
test('exr-load', async({page}) => expect(page).toHaveTitle('EXR Load'))
test('extra-importer-plugins', async({page}) => expect(page).toHaveTitle('Extra Importer Plugins'))
test('fbx-load', async({page}) => expect(page).toHaveTitle('FBX Load'))
test('gltf-animation-page-scroll', async({page}) => expect(page).toHaveTitle('GLTF Animation Page Scroll'))
test('gltf-animation-plugin', async({page}) => expect(page).toHaveTitle('GLTF Animation Plugin'))
test('gltf-camera-animation', async({page}) => expect(page).toHaveTitle('GLTF Camera Animation'))
test('gltf-load', async({page}) => expect(page).toHaveTitle('GLTF Load'))
test('gltf-transmission-test', async({page}) => expect(page).toHaveTitle('GLTF Transmission Test'))
test('half-float-hdr-test', async({page}) => expect(page).toHaveTitle('Half float HDR Test'))
test('hdr-load', async({page}) => expect(page).toHaveTitle('HDR Load'))
// hdr-to-exr — covered by interactive test
test('image-load', async({page}) => expect(page).toHaveTitle('Image Load'))
test('import-test', async({page}) => expect(page).toHaveTitle('Basic Lib Import Test'))
test('ktx-load', async({page}) => expect(page).toHaveTitle('KTX Texture Load'))
test('ktx2-load', async({page}) => expect(page).toHaveTitle('KTX2 Texture Load'))
test('material-uiconfig', async({page}) => expect(page).toHaveTitle('Material UiConfig'))
test('obj-mtl-load', async({page}) => expect(page).toHaveTitle('OBJ MTL Load'))
test('obj-to-glb', async({page}) => expect(page).toHaveTitle('OBJ To GLB'))
test('object-uiconfig', async({page}) => expect(page).toHaveTitle('Object UiConfig'))
test('parallel-asset-import', async({page}) => expect(page).toHaveTitle('Parallel Asset Import/Download'))
test('ply-load', async({page}) => expect(page).toHaveTitle('PLY Load'))
test('progressive-plugin', async({page}) => expect(page).toHaveTitle('Progressive Plugin'))
test('rhino3dm-load', async({page}) => expect(page).toHaveTitle('Rhino 3DM Load'))
test('scene-uiconfig', async({page}) => expect(page).toHaveTitle('Scene UiConfig'))
test('sphere-half-float-test', async({page}) => expect(page).toHaveTitle('Half float render pipeline test'))
test('sphere-msaa-test', async({page}) => expect(page).toHaveTitle('MSAA Test'))
test('sphere-rgbm-test', async({page}) => expect(page).toHaveTitle('RGBM Render pipeline test'))
test('stl-load', async({page}) => expect(page).toHaveTitle('STL Load'))
test('tweakpane-editor', async({page}) => expect(page).toHaveTitle('Tweakpane Editor'))
test('uint8-rgbm-hdr-test', async({page}) => expect(page).toHaveTitle('Uint8 RGBM HDR Test'))
test('usdz-load', async({page}) => expect(page).toHaveTitle('USDZ / USDA Load'))
test('viewer-uiconfig', async({page}) => expect(page).toHaveTitle('Viewer UiConfig'))
test('z-prepass', async({page}) => expect(page).toHaveTitle('Z-Prepass test'))
test('base-ground-plugin', async({page}) => expect(page).toHaveTitle('Base Ground Plugin'))
test('advanced-ground-plugin', async({page}) => expect(page).toHaveTitle('Advanced Ground Plugin'))
test('ambient-light', async({page}) => expect(page).toHaveTitle('Ambient Light'))
test('animate-material-properties', async({page}) => expect(page).toHaveTitle('Animate Material Properties'))
test('animation-clip-data', async({page}) => expect(page).toHaveTitle('Animation Clip Data'))
test('animation-object-plugin', async({page}) => expect(page).toHaveTitle('Animation Object Plugin'))
test('anisotropy-plugin', async({page}) => expect(page).toHaveTitle('Anisotropy Plugin'))
test('asset-exporter-plugin', async({page}) => expect(page).toHaveTitle('Asset Exporter Plugin'))
test('assimpjs-plugin', async({page}) => expect(page).toHaveTitle('AssimpJs Plugin'))
test('b3dm-load', async({page}) => expect(page).toHaveTitle('B3DM (Batched 3D Model, OGC Tiles) Load'))
test('basic-svg-renderer-plugin', async({page}) => expect(page).toHaveTitle('Basic SVG Renderer'))
test('blend-load', async({page}) => expect(page).toHaveTitle('Blend Load'))
test('bloom-plugin', async({page}) => expect(page).toHaveTitle('Bloom Plugin'))
test('blueprintjs-ui-plugin', async({page}) => expect(page).toHaveTitle('BlueprintJs Ui Plugin'))
test('bone-helper-widget', async({page}) => expect(page).toHaveTitle('Bone Helper Widget'))
test('camera-ortho-uiconfig', async({page}) => expect(page).toHaveTitle('Camera UiConfig'))
test('camera-view-plugin', async({page}) => expect(page).toHaveTitle('Camera View Plugin'))
test('chromatic-aberration-plugin', async({page}) => expect(page).toHaveTitle('Chromatic Aberration Plugin'))
test('clear-reimport-test', async({page}) => expect(page).toHaveTitle('Clear + Reimport Test with config'))
test('clear-reimport-test-2', async({page}) => expect(page).toHaveTitle('Clear + Reimport Test with config 2'))
test('clearcoat-tint-plugin', async({page}) => expect(page).toHaveTitle('Clearcoat Tint Plugin'))
test('cmpt-load', async({page}) => expect(page).toHaveTitle('CMPT (Composite 3D Model, OGC Tiles) Load'))
test('contact-shadow-ground-plugin', async({page}) => expect(page).toHaveTitle('Contact Shadow Ground Plugin'))
test('custom-bump-map-plugin', async({page}) => expect(page).toHaveTitle('Custom Bump Map Plugin'))
test('depthoffield-plugin', async({page}) => expect(page).toHaveTitle('DepthOfField Plugin'))
test('dispose-reimport-test', async({page}) => expect(page).toHaveTitle('Dispose and Reimport Test'))
test('dispose-reimport-test-2', async({page}) => expect(page).toHaveTitle('Dispose + Reimport Test with config'))
test('dispose-reimport-test-3', async({page}) => expect(page).toHaveTitle('Dispose + Reimport Test with config 2'))
test('dzi-load', async({page}) => expect(page).toHaveTitle('DZI (Deep Zoom Image) Load'))
test('editor-view-widget-plugin', async({page}) => expect(page).toHaveTitle('Editor View Widget Plugin'))
test('entity-component-custom', async({page}) => expect(page).toHaveTitle('Entity Component Custom'))
test('entity-component-plugin', async({page}) => expect(page).toHaveTitle('Entity Component Plugin'))
test('fat-line-spiral', async({page}) => expect(page).toHaveTitle('Fat Lines/Mesh Lines 2'))
test('fat-lines', async({page}) => expect(page).toHaveTitle('Fat Lines/Mesh Lines'))
test('fat-lines-ssr', async({page}) => expect(page).toHaveTitle('Fat/Mesh Lines SSR'))
test('fbx-export', async({page}) => expect(page).toHaveTitle('FBX Export (Assimp)'))
test('file-load', async({page}) => expect(page).toHaveTitle('File/Blob Load'))
// test('flickity-carousel', async({page}) => expect(page).toHaveTitle('Threepipe + Flickity Carousel')) // not testFinish
test('filmic-grain-plugin', async({page}) => expect(page).toHaveTitle('Filmic Grain Plugin'))
test('follow-path-constraint', async({page}) => expect(page).toHaveTitle('Follow Path Constraint'))
test('fragment-clipping-extension-plugin', async({page}) => expect(page).toHaveTitle('Fragment Clipping Extension Plugin'))
test('geometry-generator-plugin', async({page}) => expect(page).toHaveTitle('Geometry Generator Plugin'))
test('gltf-export', async({page}) => expect(page).toHaveTitle('GLTF Export'))
test('gltf-export-base-path', async({page}) => expect(page).toHaveTitle('GLTF Export'))
test('gltf-khr-material-variants-plugin', async({page}) => expect(page).toHaveTitle('glTF KHR Material Variants Plugin'))
test('gltf-mesh-lines', async({page}) => expect(page).toHaveTitle('glTF Mesh(Fat) Lines Import'))
test('gltf-meshopt-compression', async({page}) => expect(page).toHaveTitle('GLTF MeshOpt Decode Plugin (EXT_meshopt_compression)'))
test('gltf-spec-gloss-import', async({page}) => expect(page).toHaveTitle('GLB PBR SpecularGlossiness Extension Import'))
test('gltf-transmission-test-msaa-zprepass', async({page}) => expect(page).toHaveTitle('GLTF Transmission Test (MSAA + Z-Prepass)'))
test('hdri-ground-plugin', async({page}) => expect(page).toHaveTitle('HDRi Ground Plugin'))
test('hemisphere-light', async({page}) => expect(page).toHaveTitle('Hemisphere Light'))
test('hierarchy-ui-plugin', async({page}) => expect(page).toHaveTitle('Hierarchy UI Plugin'))
// test('html-js-sample', async({page}) => expect(page).toHaveTitle('Threepipe HTML/JS Sample')) // sample - no testFinish
test('i3dm-load', async({page}) => expect(page).toHaveTitle('I3DM (Instanced 3D Model, OGC Tiles) Load'))
test('interaction-prompt-plugin', async({page}) => expect(page).toHaveTitle('Interaction Prompt Plugin'))
test('js-image-data-test', async({page}) => expect(page).toHaveTitle('JS ImageData test'))
test('line-helper-widget', async({page}) => expect(page).toHaveTitle('Line Helper Widget'))
test('loading-screen-plugin', async({page}) => expect(page).toHaveTitle('Loading Screen Plugin'))
test('meshopt-simplify-modifier-plugin', async({page}) => expect(page).toHaveTitle('MeshOpt Simplify Modifier Plugin'))
test('model-viewer', async({page}) => expect(page).toHaveTitle('Model Viewer Sample'))
// test('monkey-type-3d', async({page}) => expect(page).toHaveTitle('Monkey Type 3D'))
test('msaa-ssaa', async({page}) => expect(page).toHaveTitle('MSAA + SSAA'))
test('multi-viewer-test', async({page}) => expect(page).toHaveTitle('Multiple Viewers Test'))
test('noise-bump-material-plugin', async({page}) => expect(page).toHaveTitle('SparkleBump(NoiseBump) Material Plugin'))
test('object-constraints-plugin', async({page}) => expect(page).toHaveTitle('Object Constraints Plugin'))
test('object3d-generator-plugin', async({page}) => expect(page).toHaveTitle('Object3D Generator Plugin'))
test('object3d-widgets-plugin', async({page}) => expect(page).toHaveTitle('Object3D Widgets Plugin'))
test('outline-plugin', async({page}) => expect(page).toHaveTitle('Outline Plugin'))
test('parallax-mapping-plugin', async({page}) => expect(page).toHaveTitle('Parallax Bump Mapping (Relief)'))
test('pivot-controls-plugin', async({page}) => expect(page).toHaveTitle('Pivot Controls Plugin'))
test('pivot-edit-plugin', async({page}) => expect(page).toHaveTitle('Pivot Edit Plugin'))
test('pnts-load', async({page}) => expect(page).toHaveTitle('PNTS Load'))
test('point-light', async({page}) => expect(page).toHaveTitle('Point Light'))
test('progressive-hdr-shadows-exp', async({page}) => expect(page).toHaveTitle('Progressive HDR Environment Shadows (Experiment)'))
test('rect-area-light', async({page}) => expect(page).toHaveTitle('Rect Area Light'))
test('reimport-duplicate-test', async({page}) => expect(page).toHaveTitle('Reimport materials test'))
test('screen-pass-extension', async({page}) => expect(page).toHaveTitle('Screen Pass Extension'))
test('screen-shader', async({page}) => expect(page).toHaveTitle('Screen Shader'))
test('screen-shader-advanced', async({page}) => expect(page).toHaveTitle('Screen Shader Advanced'))
test('screen-shader-material', async({page}) => expect(page).toHaveTitle('Screen Shader Material'))
test('shape-tube-extrude', async({page}) => expect(page).toHaveTitle('Shape Tube Extrude Plugin'))
test('shape-tube-extrude-plugin', async({page}) => expect(page).toHaveTitle('Shape Tube Extrude Plugin'))
test('simple-svg-text-plane', async({page}) => expect(page).toHaveTitle('Simple Text Plane (svg)'))
test('simplify-modifier-plugin', async({page}) => expect(page).toHaveTitle('Simplify Modifier Plugin'))
test('skeleton-helper-widget', async({page}) => expect(page).toHaveTitle('Skeleton Helper Widget'))
test('sky-shader-simple', async({page}) => expect(page).toHaveTitle('Sky Shader (Simple)'))
test('sky-shader-simple-ts', async({page}) => expect(page).toHaveTitle('Sky Shader (Simple)'))
test('splat-load', async({page}) => expect(page).toHaveTitle('Gaussian Splat Load'))
test('spot-light', async({page}) => expect(page).toHaveTitle('Spot Light'))
test('ssaa-plugin', async({page}) => expect(page).toHaveTitle('SSAA Plugin'))
test('ssao-plugin', async({page}) => expect(page).toHaveTitle('SSAO Plugin'))
test('ssgi-plugin', async({page}) => expect(page).toHaveTitle('SSGI Plugin'))
test('ssgi-ssr-plugin', async({page}) => expect(page).toHaveTitle('SSGI + SSR Plugins'))
test('ssreflection-plugin', async({page}) => expect(page).toHaveTitle('SSReflection Plugin'))
test('sscontactshadows-plugin', async({page}) => expect(page).toHaveTitle('SS Contact Shadows Plugin'))
test('stencil-picking-outline', async({page}) => expect(page).toHaveTitle('Stencil Picking Outline'))
test('svg-geometry-playground', async({page}) => expect(page).toHaveTitle('SVG Geometry Renderer'))
test('switch-node-plugin', async({page}) => expect(page).toHaveTitle('Switch Node Plugin (Configurator)'))
test('tailwind-css-cdn-plugin', async({page}) => expect(page).toHaveTitle('TailwindCSS CDN Plugin'))
test('temporalaa-plugin', async({page}) => expect(page).toHaveTitle('TemporalAA Plugin'))
test('theatrejs-plugin', async({page}) => expect(page).toHaveTitle('TheatreJs Plugin Test'))
test('three-csm-basic', async({page}) => expect(page).toHaveTitle('Three CSM Basic Demo (Cascaded Shadow Maps)'))
test('three-gpu-pathtracer', async({page}) => expect(page).toHaveTitle('Three GPU Path Tracer')) // todo timeout or something
test('three-svg-renderer-plugin', async({page}) => expect(page).toHaveTitle('Three SVG Renderer'))
test('timeline-ui-plugin', async({page}) => expect(page).toHaveTitle('Timeline UI Plugin'))
test('transform-animation-plugin', async({page}) => expect(page).toHaveTitle('Transform Animation Plugin'))
test('troika-text-plane', async({page}) => expect(page).toHaveTitle('Troika Three Text Plane'))
test('troika-text-plugin', async({page}) => expect(page).toHaveTitle('Troika Text Plugin'))
test('troika-text-shadow', async({page}) => expect(page).toHaveTitle('Troika Three Text With Shadow'))
test('tweakpane-ui-plugin', async({page}) => expect(page).toHaveTitle('Tweakpane Ui Plugin'))
test('velocity-buffer-plugin', async({page}) => expect(page).toHaveTitle('Velocity Buffer Plugin'))
test('viewer-render-size', async({page}) => expect(page).toHaveTitle('Viewer Render Size'))
test('vignette-plugin', async({page}) => expect(page).toHaveTitle('Vignette Plugin'))
test('virtual-camera', async({page}) => expect(page).toHaveTitle('Virtual Camera'))
test('virtual-cameras-plugin', async({page}) => expect(page).toHaveTitle('Virtual Cameras Plugin'))
test('watch-hands-plugin', async({page}) => expect(page).toHaveTitle('Watch Hands Plugin'))

// ─── Framework samples (React, R3F, Vue, Svelte) ────────────────────────────
test('r3f-jsx-sample', async({page}) => expect(page).toHaveTitle('Threepipe R3F React/JSX Sample'))
test('r3f-tsx-sample', async({page}) => expect(page).toHaveTitle('Threepipe React/TSX Sample'))
test('r3f-js-sample', async({page}) => expect(page).toHaveTitle('Threepipe React/JS Sample'))
test('r3f-jsx-webgi', async({page}) => expect(page).toHaveTitle('Threepipe R3F React/JSX + WebGI'))
test('r3f-tsx-webgi', async({page}) => expect(page).toHaveTitle('Threepipe R3F React/TSX + WebGi'))
test('r3f-diamond-caustics-demo', async({page}) => expect(page).toHaveTitle('Threepipe R3F Drei Diamond Caustics Demo'))
test('r3f-drei-text-html', async({page}) => expect(page).toHaveTitle('Threepipe R3F Router Transitions'))
test('r3f-drei-transform-controls', async({page}) => expect(page).toHaveTitle('Threepipe R3F Drei TransformControls'))
test('r3f-pivot-controls', async({page}) => expect(page).toHaveTitle('Threepipe R3F Drei PivotControls'))
test('r3f-router-transitions-demo', async({page}) => expect(page).toHaveTitle('Threepipe R3F Router Transitions'))
// test('r3f-spline-glass-demo', async({page}) => expect(page).toHaveTitle('Threepipe R3F Spline Glass Demo')) // broken: WebGLMultipleRenderTargets removed from three.js fork
test('r3f-ssr-demo', async({page}) => expect(page).toHaveTitle('Threepipe R3F Screen Space Reflections'))
test('react-js-sample', async({page}) => expect(page).toHaveTitle('Threepipe React/JS Sample'))
test('react-jsx-sample', async({page}) => expect(page).toHaveTitle('Threepipe React/JSX Sample'))
test('react-tsx-sample', async({page}) => expect(page).toHaveTitle('Threepipe React/TSX Sample'))
test('vue-html-sample', async({page}) => expect(page).toHaveTitle('Threepipe Vue/HTML Sample'))
test('vue-sfc-sample', async({page}) => expect(page).toHaveTitle('Threepipe React/TSX Sample'))
test('svelte-sample', async({page}) => expect(page).toHaveTitle('Threepipe Svelte Sample'))
